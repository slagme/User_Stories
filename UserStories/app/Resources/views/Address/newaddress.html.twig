{% extends '::base.html.twig' %}

{% block body %}
    {{ form_start (formAddress.city) }}
    {{ form_start (formAddress.street) }}
    {{ form_start (formAddress.houseNumber) }}
    {{ form_start (formAddress.houseUnitNumber) }}
    {{ form_widget(formAddress) }}
    <input type="submit" value="Add new address">
    {{ form_end(formAddress) }}
{% endblock %}